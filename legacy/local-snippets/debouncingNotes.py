


# '=' is used for defining variables, '==' is used for comparisons


# In functions, variables must be defined as global if it is altered
# within the function, and then used outside of the function.
# Otherwise, the changes made within the function wouldn't stay after
# the function executes

# creates the timer
# initiates the timer, period is in milliseconds, mode can
# be .PERIODIC or .ONE_SHOT, callback is the function that it executes
# (callback function must be kept short)
